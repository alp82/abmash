package com.abmash.extraction;


public class TableExtractor {
	

	protected void perform() {
//		AbstractTarget foundTarget = new TargetFinder(webDriver, target + " td").getFirst();
		
		// TODO table finder logic
		//1. if(!target.contains("table")) target = target + "table";
		//2. target = "table";
		
//		List<WebElement> captions = webDriver.findElements(foundTarget.getLocator());
//		for (WebElement element : captions) {
//			RenderedWebElement renderedElement = (RenderedWebElement) element;
//			browser.log().debug("text: " + element.getText());
//			browser.log().debug("location: " + renderedElement.getLocation());
//			browser.log().debug("size: " + renderedElement.getSize());
//		}		
	}
	
}
